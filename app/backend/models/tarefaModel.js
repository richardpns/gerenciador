const db = require('../config/dbConfig');

class Tarefa {
    static criarTarefa(tarefa, callback) {
        // Lógica para criar uma nova tarefa no banco de dados
    }

    static obterTarefaPorId(tarefaId, callback) {
        // Lógica para obter uma tarefa por ID do banco de dados
    }

}

module.exports = Tarefa;
